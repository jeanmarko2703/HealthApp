import 'package:flutter/material.dart';

class SiginScreen extends StatelessWidget {
  const SiginScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SiginScreen'),
      ),
    );
  }
}
